const Note = require('./../../model/Note');
const checkAuth = require('./../../utils/check_auth')

//NOTE: only finish putting function that is needed
module.exports = {
    Query: {
        getNote: async (_, __, context) => {}
    },
    Mutation: {
        updateNote: async (_, { msg }, context) => {}
    }
}